const text="Sévigné — La semaine";const el=document.getElementById("typed");let i=0;function type(){if(i<text.length){el.textContent+=text[i++];setTimeout(type,55)}}type();if("serviceWorker"in navigator){navigator.serviceWorker.register("sw.js").catch(()=>{})}